<header class="header-container">
  <div class="header">
    <div class="top-bar">
        <div class="top-left">
        <p class="bold">For emergency cases: <span class="underline">800 123 45 67 </span></p>
        <p>Location: 121 Wallstreet Street, NY, USA</p>
        <p>Open hours: 9:00–24:00 Mon–Sat</p>
        <p>Email: info@ddsngr.com</p>
        </div>

        <div class="top-right">
        <a href="#"><img src="/src/assets/facebook.svg" /></a>
        <a href="#"><img src="/src/assets/insta.svg" /></a>
        <a href="#"><img src="/src/assets/x.svg" /></a>
        <a href="#"><img src="/src/assets/linkedIn.svg" /></a>
        </div>
    </div>

    <div class="main-bar">
        <div class="logo">
            <img src="/src/assets/logo-mark.svg" alt="Ddsgnr logo" style="height:40px;" />
            <span class="logo-text bold">Ddsngr</span>
        </div>

        <input type="checkbox" id="menu-toggle" />
        <label for="menu-toggle" class="burger">
            <span></span>
            <span></span>
            <span></span>
        </label>

        <div class="right-side">
            <nav class="nav">
                <a href="/">Home</a>
                <a href="/services">Services</a>
                <a href="/about">About</a>
                <a href="/contact">Contact</a>
            </nav>

            <a class="cta" href="/contact">
                Book Your Appointment Now
            </a>
        </div>
    </div>
    </div>
</header>

<style>
    #menu-toggle {
        display: none;
    }

    .burger {
        display: none;
        flex-direction: column;
        gap: 5px;
        cursor: pointer;
    }
    .burger span {
        width: 24px;
        height: 2.5px;
        background: #000;
        border-radius: 2px;
        transition: 0.3s ease;
    }

    .header-container {
        display: flex;
        justify-content: center;
        align-items: center;
    }
    .header {
        font-family: roboto, system-ui, sans-serif;
        width: 95%;
        margin-bottom: 1rem
    }
    .bold {
        font-weight: 500
    }

    .underline {
        text-decoration: underline
    }
    .top-bar {
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.8rem;
        border-bottom: 1px solid #ddd;
        padding-bottom: 1rem;
    }

    .top-left {
        display: flex;
        gap: 1.5rem;
        flex-wrap: wrap;
        margin-left: 1rem
    }

    .main-bar {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0.5rem 2rem;
        background: #fff;
    }

    .top-right {
        display: flex;
        gap: 0.6rem;
    }

    .right-side {
        display: flex;
        justify-content: center;
        gap: 1rem;
        align-items: center;
    }

    .nav {
        display: flex;
        gap: 2rem;
    }
    .nav a {
        text-decoration: none;
        color: #000;
        font-weight: 500;
    }

    .cta {
        background: #000;
        color: #fff;
        padding: 0.75rem 1.25rem;
        border-radius: 4px;
        text-decoration: none;
        font-size: 0.9rem;
        font-weight: 600;
    }

    .top-bar p {
        margin: 0;
    }

    .logo {
        display: flex;
        align-items: center;
        gap: 0.5rem;
  }

  @media (max-width: 900px) {
  .top-bar {
    display: none;
  }

  .burger {
    display: flex;
  }

  .right-side {
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background: #fff;

    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    padding: 2rem 0;

    border-top: 1px solid #eee;

    opacity: 0;
    pointer-events: none;
    transform: translateY(-10px);
    transition: 0.3s ease;
  }

  .nav {
    flex-direction: column;
    gap: 1.2rem;
  }

  .cta {
    width: 85%;
    text-align: center;
  }

  /* OPEN MENU */
  #menu-toggle:checked ~ .right-side {
    opacity: 1;
    pointer-events: auto;
    transform: translateY(0);
  }

  /* BURGER → X */
  #menu-toggle:checked + .burger span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  #menu-toggle:checked + .burger span:nth-child(2) {
    opacity: 0;
  }

  #menu-toggle:checked + .burger span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
  }
}
</style>